# Spring Boot Debug Module Configuration
# 用于调试和阅读源码的配置文件

spring:
  application:
    name: my-debug-module

# 服务器配置
server:
  port: 8080
#  context-path: /

# 日志配置
logging:
  level:
    root: INFO
    org.springframework.boot: DEBUG
    org.springframework.web: DEBUG
    com.debug: DEBUG

